## 2024-05-14 - Replace @import with next/font/google
**Learning:** Using `@import` in global CSS for Google Fonts blocks the render pipeline and reduces Lighthouse performance scores.
**Action:** Replace `@import` with `next/font/google` in Next.js applications for better font optimization, faster loading, and preventing render-blocking resources.
