## 2024-05-19 - Keyboard Navigation Focus States
**Learning:** The application lacked visual indicators for keyboard focus across all interactive elements, making it difficult for keyboard users to navigate.
**Action:** Added global `:focus-visible` styles using the existing accent color in `globals.css` to ensure keyboard accessibility while preserving a clean UI for mouse users.
